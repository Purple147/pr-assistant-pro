# Admin Guide — PR Assistant PRO
1. Edit .env with your GITHUB_APP_ID and PRIVATE_KEY path.
2. Start: `docker-compose up -d --build`
3. Logs: `docker-compose logs -f web`
4. Healthcheck endpoint: GET /health
5. Backup: DB file at /data/db.sqlite
